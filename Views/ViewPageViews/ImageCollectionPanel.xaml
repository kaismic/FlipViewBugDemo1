<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FlipViewBugDemo1.Views.ViewPageViews.ImageCollectionPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:entities="using:FlipViewBugDemo1.Entities"
    >
    <Viewbox>
        <ListView ItemsSource="{x:Bind ViewModel.ImageInfos}"
                  SelectionMode="None" IsItemClickEnabled="False">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entities:ImageInfo">
                    <Image>
                        <Image.Source>
                            <BitmapImage UriSource="{x:Bind ImageFilePath}"></BitmapImage>
                        </Image.Source>
                    </Image>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerTransitions>
                <TransitionCollection/>
            </ListView.ItemContainerTransitions>
        </ListView>
    </Viewbox>
</UserControl>